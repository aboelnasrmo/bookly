import 'package:flutter/material.dart';

const Color kMyBackGround = Color(0xff100B20);
const kTransitionDuration = Duration(seconds: 2);
const Color kMyBookBackGroundColor = Colors.orange;
const Color kMyWhite = Colors.white;
